[pytest]
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
testpaths = tests
markers =
    integration: integration tests
python_files =
    test_*.py
    *_tests.py
    tests.py
addopts = 
    --maxfail=1 
    --capture=no 
    --tb=auto 
    --cov=hey_robot
    --cov-report=term-missing:skip-covered 
    --cov-report=xml:artifacts/test/cov.xml 
    --junitxml=artifacts/test/report.xml 
    --cov-fail-under=85
    -vv
asyncio_mode = auto
env =
    COVERAGE_FILE=artifacts/test/.coverage
    # Tests run in headless environments that are not guaranteed to expose an
    # EGL platform device (for example CPU-only CI containers).  MuJoCo selects
    # its GLContext implementation at import time, so use the portable software
    # renderer for the whole pytest process instead of attempting a late EGL
    # fallback in the simulation driver.
    MUJOCO_GL=osmesa
    server__use_ray=false
    server__enable_proxy=true
    load_builtin_plugins=["echo","proxy"]
    sentry__enable=false
    test__silent=true
